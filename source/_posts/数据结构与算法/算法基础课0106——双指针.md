title: 算法基础课0106——双指针
date: '2021-06-16 17:26:18'
updated: '2021-06-19 16:40:11'
tags: [双指针, 算法基础课, 算法, algorithm]
categories: 算法
permalink: /articles/2021/06/16/1623854383506.html
cover: https://b3logfile.com/bing/20181031.jpg?imageView2/1/w/960/h/540/interlace/1/q/100
---

# 双指针

算法介绍:

## 题目一 最长连续不重复子串

```c++
#include <iostream>
#include <algorithm>

using namespace std;
const int N = 1e5+10;
int a[N],s[N];

int main(){
  int n;
  cin>>n;
  for(int i=1;i<=n;i++) scanf("%d",&a[i]);
  int l =1, r = n;
  int ans = 0;
  
  for(int i=1,j=1;j<=n;j++){
    s[a[j]]++;
    while(i<j&&s[a[j]]>1) s[a[i++]]--;
    ans  = max(ans,j-i+1);
  }
  printf("%d\n",ans);
  return 0;
}
```

## 题目二 数组元素的目标和

```c++
#include <iostream>
#include <algorithm>

using namespace std;

const int N = 1e5+10;

int a[N],b[N];

int main(){
  int n,m,k;
  cin>>n>>m>>k;
  for(int i=0;i<n;i++)scanf("%d",&a[i]);
  for(int i=0;i<m;i++)scanf("%d",&b[i]);
  int j = m-1;
  int l=0,r=0;
  for(int i=0;i<n;i++){
    while(a[i]+b[j]>k&&j>=0) j--;
    if(a[i]+b[j]==k){
      l=i,r=j;
      break;
    }
  }
  printf("%d %d\n",l,r);
  return 0;
}
```

## 题目三  判断子序列

```c++
#include <iostream>
#include <algorithm>

using namespace std;

const int N=1e5+10;

int a[N],b[N];

int main(){
  int n,m;
  cin>>m>>n;
  for(int i=0;i<m;i++) scanf("%d",&b[i]);
  for(int i=0;i<n;i++) scanf("%d",&a[i]);
  int i,j;
  for(i=0,j=0;i<n;i++){
      while(a[i]==b[j]&&j<m&&i<n) j++,i++;
  }
  // cout<<i<<" "<<j<<endl;
  if(j==m)
    printf("Yes\n");
  else
    printf("No\n");
  return 0;
}
```
